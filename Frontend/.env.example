# Frontend environment variables
# Option A: Use Vite proxy (recommended for local dev). Leave these unset and the app will call '/api' and '/uploads' via proxy.
#
# Option B: Direct API URL (no proxy). Uncomment and set these to point directly to your backend.
# VITE_API_URL=http://localhost:5000/api
# VITE_API_ORIGIN=http://localhost:5000
